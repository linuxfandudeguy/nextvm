# .github/dependabot.yml

version: 2
updates:
  - package-ecosystem: "bun" # Ecosystem for managing dependencies (bun, npm, yarn, etc.)
    directory: "/"            # Location of the package manifest (e.g., package.json or bun.lockb)
    schedule:
      interval: "weekly"      # How often to check for updates (daily, weekly, monthly)
    commit-message:
      prefix: "[🤖] Update"   # Prefix for the commit message generated by Dependabot
